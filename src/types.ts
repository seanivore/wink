export interface Question {
  question: string;
  answers: string[];
}

export interface ArtStyle {
  name: string;
  description: string;
  imageUrl: string;
  artists: string[];
  learnMoreUrl: string;
}